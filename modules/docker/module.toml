name = "docker"
depends_on = ["core"]
notes = [
    "Run `sudo usermod -aG docker $USER` to add your user to the docker group. Log out and back in for this to take effect.",
]


[[commands]]
run = """
curl -fsSL https://get.docker.com | bash
"""
unless = "command -v docker"
requires = ["bash", "curl"]
