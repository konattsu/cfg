name = "keychain"
depends_on = ["core"]
notes = [
  "keychain に読み込ませる SSH key は必要に応じて手動で追加する。",
]

[packages]
apt = ["keychain"]

[[blocks]]
src = "files/keychain.sh"
dst = "~/.zshrc"
marker = "cfg:keychain"

[[blocks]]
src = "files/keychain.sh"
dst = "~/.bashrc"
marker = "cfg:keychain"

